{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "myChannels Debug",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/myChannels",
            "args": ["2", "1", "input_files/inputs/metadata.txt", "1", "1", "output.txt"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
